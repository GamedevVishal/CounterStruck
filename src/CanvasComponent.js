
import React from 'react';
import { useThree } from '@react-three/fiber';

function CanvasComponent() {
    const { scene } = useThree();

    // Add Three.js objects to the scene here

    return null;
}

export default CanvasComponent;
